# pstream-app

Open-source mobile client for P-Stream. Native React Native UI (TypeScript).
Default mode: app connects to a hosted P-Stream instance (self-hosted recommended).
Supports user-switchable instance URLs so anyone can use their own backend + proxy.
